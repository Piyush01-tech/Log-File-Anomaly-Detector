"""
dashboard/admin.py  [PLACEHOLDER — Phase 9]
=============================================
Django admin registrations.

Phase 9 will register the Anomaly model here so that
records can be inspected via /admin/ during development.
"""

from django.contrib import admin

# Phase 9 will add:
# from .models import Anomaly
# @admin.register(Anomaly)
# class AnomalyAdmin(admin.ModelAdmin): ...
