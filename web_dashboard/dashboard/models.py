"""
dashboard/models.py  [PLACEHOLDER — Phase 9]
=============================================
Django ORM models for the anomaly detection dashboard.

Full implementation arrives in Phase 9.

The Anomaly model will store:
  - timestamp, computer, event_count
  - failed_logins, anomaly_score, severity
  - created_at

Designed for SQLite now, PostgreSQL later.
"""

from django.db import models


# Phase 9 will implement:
# class Anomaly(models.Model):
#     timestamp = models.DateTimeField(...)
#     computer = models.CharField(...)
#     ...
