# This directory holds trained model artifacts:
#   isolation_model.joblib  — Trained Isolation Forest model
#   scaler.joblib           — Fitted StandardScaler
#
# These files are generated by running:
#   python ml_engine/train.py
#
# They are intentionally excluded from git (.gitignore).
# In a production environment, store them in an artifact registry
# (e.g., MLflow, AWS S3, Azure Blob Storage).
