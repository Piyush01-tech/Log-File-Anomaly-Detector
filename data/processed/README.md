# Processed data directory.
#
# Contains:
#   parsed_events.csv     — Output of parser.py (raw records as CSV)
#   feature_matrix.csv    — Output of feature_engineering.py (ML-ready)
#
# Generated automatically during the pipeline run.
# Excluded from git to avoid committing sensitive log data.
